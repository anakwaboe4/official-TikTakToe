export const Board = (props: {
    squares: number[][],
}) => {
    const {
        squares
    } = props;

    // State

    // Functions

    // Effects

    return (
        <div>
            testboard
            {squares}
        </div>
    );
}